{
  "name": "yt_disruptor_root",
  "version": "1.0.0",
  "description": "FSD Kinetic Bridge",
  "main": "index.js",
  "scripts": {
    "start": "cd backend && node server.js",
    "install-all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "axios": "^1.6.0",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "mongoose": "^7.5.0",
    "cors": "^2.8.5"
  }
}